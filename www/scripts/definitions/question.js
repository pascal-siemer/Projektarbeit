class Question {
    constructor(prompt, answers, indexCorrect) {
        this.prompt = prompt;        //string
        this.answers = answers;       //List<string>
        this.indexCorrect = indexCorrect;  //int
    }
}